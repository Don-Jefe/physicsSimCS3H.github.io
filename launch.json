{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Open Chrome (No Port)",
            "type": "chrome",
            "request": "launch",
"file":"${workspaceFolder}/index.html"

            
        }
    ]
}